# Render.com config cho RescueLink Backend
# File này chỉ để tham khảo — Render đọc package.json tự động

# Build command: npm install
# Start command: node src/app.js
# Node version: 20

# Env vars cần set trên Render dashboard:
# MONGODB_URI     = mongodb+srv://...
# JWT_SECRET      = rescuelink_jwt_secret_key_2026_xyz
# NODE_ENV        = production
# PORT            = (Render tự set, không cần)
# TWILIO_ACCOUNT_SID  = (sau khi có)
# TWILIO_AUTH_TOKEN   = (sau khi có)
# TWILIO_PHONE_NUMBER = (sau khi có)
